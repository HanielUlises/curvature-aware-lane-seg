defaults:
  - data: curvelanes
  - subset: default
  - model: unet_resnet18
  - train: default
  - infer: default
  - _self_

seed: 42

paths:
  data_root: data/raw/Curvelanes
  output_root: data/raw/prepared

hydra:
  job:
    chdir: false
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}
